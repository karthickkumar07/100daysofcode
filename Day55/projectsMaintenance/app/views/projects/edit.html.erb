<h1> Edit Project </h1>
<%= form_for :project, url:project_path(@project), method: :patch do |p| %>
  <p>
    <%= p.label :title %><br>
    <%= p.text_field :title %>
  </p>

  <p>
    <%= p.label :body %><br>
    <%= p.text_area :body %>
  </p>

  <p>
     <%= p.submit %>
  </p>

<% end %>
